load("//bazel:defs.bzl", "crate_library", "crate_tests")

package(default_visibility = ["//visibility:public"])

# Consumed by `crate.from_cargo` (see //MODULE.bazel).
exports_files(["Cargo.toml"])

crate_library(
    name = "pprof",
    # `build.rs` reaches this with `include_str!`, so it is read while the
    # script compiles rather than while it runs.
    build_script_compile_data = ["src/proto_mapping.rsfrag"],
    # `build.rs` generates prost types from these.
    build_script_data = glob(["proto/**"]),
)

crate_tests(
    lib = "pprof",
)
